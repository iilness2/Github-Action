name: filter-by-path-workflow
on:
 push:
  branches:
    - CS-*-MIYAZAKI-NETWORK
  paths:
      # '*' matches any character except '/'
      - 'services/miyazaki/network/*' 
jobs:
 run-the-job:
    runs-on: ubuntu-latest
    steps:
    - name: Dump Echo
      run: echo "edited"