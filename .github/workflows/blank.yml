name: CI

on: [push]
#  push:
#    branches:
#    - master
#    paths:
#       - .
jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
#    - name: try choose folder
#      uses: ./
    - name: Run terraform version
      run: cd .. && terraform version
  go:
    runs-on: ubuntu-latest
    steps:
#    - name: try choose folder
#      uses: ./go/bin
    - name: Run go version
      run: cd .. && go version
