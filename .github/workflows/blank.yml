name: CI

on: [push]

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
    - name: Run terraform version
      run: terra/terraform version
    - run: ls -l
  go:
    runs-on: ubuntu-latest
    steps:
    - name: Run go version
      run: go/bin/go version
