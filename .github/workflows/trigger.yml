name: trigger

on: [pull_request]

jobs:
#  terraform:
#    runs-on: ubuntu-latest
#    steps:
#    - name: Using terraform
#      uses: hashicorp/terraform-github-actions/plan@v0.3.5
#    - name: Check terraform version
#      run: terraform version

 setup-go:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    # - uses: actions/setup-go@v1
    #   with:
    #     go-version: '1.11.x' # The Go version to download (if necessary) and use.
    # - run: go run main.go
    - name: Check go files
      run: ls -al 
    - name: Check go version
      run: go version


 check-go-file:
   needs: setup-go
   runs-on: ubuntu-latest
   steps:
   - name: Change Name
     run: ls -al